[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    kis_live: hits real KIS demo API — requires .env, run manually only
    toss_live: hits real Toss API (read-only) — requires .env, run manually only
addopts = -m "not kis_live and not toss_live"
