# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.flox/

# uv temp lock (use canonical uv.lock only)
uv-*.lock

# Secrets
.env
.env.*
!.env.example

# Per-broker runtime cache: OAuth tokens, approval keys, captured raw responses
.kis-spec/

# Superpowers working docs (specs/plans) — local-only, do not commit
docs/superpowers/

# Claude Code local session/agent state
.claude/
